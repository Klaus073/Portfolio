name: Deploy to Staging with Docker

on:
  push:
    branches: [ main ]

env:
  IMAGE_NAME: 'protfolio'  # Updated to match your compose file
  CONTAINER_NAME: 'protfolio-app'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Configure Docker for insecure registry
      run: |
        # Configure Docker daemon to allow insecure registry
        sudo mkdir -p /etc/docker
        echo '{
          "insecure-registries": ["${{ secrets.REGISTRY_URL }}"]
        }' | sudo tee /etc/docker/daemon.json
        sudo systemctl restart docker
        
        # Wait for Docker to restart
        sleep 10
    
    - name: Build Docker image
      run: |
        docker build -t ${{ env.IMAGE_NAME }}:latest -f ./portfolio-app/Dockerfile ./portfolio-app
        docker tag ${{ env.IMAGE_NAME }}:latest ${{ secrets.REGISTRY_URL }}/${{ env.IMAGE_NAME }}:latest
        docker tag ${{ env.IMAGE_NAME }}:latest ${{ secrets.REGISTRY_URL }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
    
    - name: Push to Registry
      run: |
        docker push ${{ secrets.REGISTRY_URL }}/${{ env.IMAGE_NAME }}:latest
        docker push ${{ secrets.REGISTRY_URL }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
    
    - name: Deploy to Staging Server
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ${{ secrets.EC2_USER }}
        key: ${{ secrets.EC2_SSH_KEY }}
        port: ${{ secrets.STAGING_EC2_PORT || 22 }}
        script: |
          # Navigate to the ranking folder
          cd /home/ubuntu/Ui-Protfolio
          
          # Derive registry and set compose image vars
          REG="${{ secrets.REGISTRY_URL }}"
          REG="${REG%/}"
          export IMAGE="$REG/${{ env.IMAGE_NAME }}"
          export IMAGE_TAG=latest

          # Pull the latest image from registry
          sudo docker pull "$IMAGE:$IMAGE_TAG"

          # Stop current containers (docker compose)
          sudo docker compose down || true

          # Start containers with the new image
          sudo docker compose up -d --remove-orphans
          
          # Wait for containers to be healthy
          sleep 5
          
          # Check if containers are running
          if sudo docker compose ps | grep -q "Up"; then
            echo "✅ Deployment successful"
            
            # Clean up old images (keep only current and one previous)
            echo "Cleaning up old Docker images..."
            
            # Get current image ID
            CURRENT_IMAGE=$(sudo docker inspect --format='{{.Id}}' "$IMAGE:$IMAGE_TAG")
            
            # Keep only the last 2 images of our app
            sudo docker images --format "{{.Repository}}:{{.Tag}} {{.ID}}" | \
              grep "$REG/${{ env.IMAGE_NAME }}" | \
              grep -v "latest" | \
              awk '{print $2}' | \
              xargs -r sudo docker rmi -f 2>/dev/null || true
            
            # Remove dangling images
            sudo docker image prune -f
            
            echo "✅ Cleanup complete"
          else
            echo "❌ Deployment failed! Check docker compose logs"
            sudo docker compose logs --tail=50
            exit 1
          fi